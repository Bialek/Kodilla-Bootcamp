// Import Actions
import {  } from './KanbanActions';

// Initial State
const initialState = {};

const KanbanReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default KanbanReducer;
